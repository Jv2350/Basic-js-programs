// area of trinangle

const base = prompt("Enter base of triangle: ");
const height = prompt("Enter height of trinangle: ");

const area = (1 / 2) * (base * height);

console.log(`Area of trinangle is ${area}`);
